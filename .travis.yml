language: node_js
node_js:
 #- "node"
 # Node 11.7.0 breaks code coverage
- "11.6.0"
services:
 - xvfb
script:
 - npm test
 - npm run test-int
 - npm run test-e2e
 - npm run eslint
after_success:
 - npm run coveralls
before_cache:
 - rm -rf $HOME/build/nkl-kst/MagicMirror/modules/MMM-RBB-Weather
cache:
 directories:
  - node_modules
  - $HOME/build/nkl-kst/MagicMirror
